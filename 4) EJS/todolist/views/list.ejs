<%- include('header'); -%>

    <div class="box" id="heading">
      <h1>Today is <%= ListTitle %></h1>
    </div>

      <!-- <% if(ListTitle==="Sunday" || ListTitle==="Saturday"){%>
    <h1 style="color: red"><%=ListTitle%> TODO List</h1>
    <%}else{%>
    <h1 style="color: blueviolet"><%=ListTitle%> TODO List</h1>
    <%}%> -->

    <div class="box">
      <% for(let i=0;i<newList.length;i++){ %>
        <div class="item">
          <input type="checkbox">
          <p><%= newList[i] %></p>
        </div>
      <%}%>

      <form class="item" action="/" method="post">
        <input name="newItem" type="text" placeholder="New Item" autocomplete="off"/>
        <button name="list" type="submit" value=<%= ListTitle %> >+</button>
      </form>
    </div>

<%- include('footer'); -%>
